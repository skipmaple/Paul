- body_classes = %w[bg-local bg-no-repeat bg-light-100 dark:bg-dark-600]
- body_classes << 'bg-body' unless content_for?(:no_body_bg)

!!!5
%html{ lang: I18n.locale }
  = render 'layouts/head'

  %body{ class: body_classes }
    = render 'layouts/page', sidebar: sidebar, nav: nav
  -# = debug(params) if Rails.env.development?
